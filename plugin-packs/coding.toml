# coding — Full-stack AI coding pack with BMAD agents
# Install with: pulse plugin install-pack coding

[pack]
name = "coding"
description = "Full-stack AI coding: Claude Code + BMAD agents + Git automation"
version = "0.1.0"
author = "Jack"

[plugins]
provider-claude-code = { version = ">=0.1.0", source = "local:../../provider-claude-code" }
plugin-git-ops       = { version = ">=0.1.0", source = "local:../../git-ops" }
plugin-git-worktree  = { version = ">=0.1.0", source = "local:../../git-worktree" }
bmad-method          = { version = ">=0.1.0", source = "local:../../bmad-method" }
plugin-board         = { version = ">=0.1.0", source = "local:../../plugin-board", optional = true }

[workflows]
include = [
  "config/workflows/coding-feature-dev.yaml",
  "config/workflows/coding-bug-fix.yaml",
  "config/workflows/coding-review.yaml",
  "config/workflows/coding-refactor.yaml",
  "config/workflows/coding-story-dev.yaml",
  "config/workflows/coding-quick-dev.yaml",
  "config/workflows/bootstrap-plugin.yaml",
  "config/workflows/bootstrap-rebuild.yaml",
  "config/workflows/bootstrap-cycle.yaml",
]

[dashboard]
has_extension = true
source = "dashboard/"
manifest = "manifest.json"
display_customizations = "display-customizations.json"

# All pages are SDK-rendered via manifest (auto-discovered by dashboard)
# detail: /plugins/plugin-coding-pack/overview
# table:  /plugins/plugin-coding-pack/workflows
# table:  /plugins/plugin-coding-pack/agents
# detail: /plugins/plugin-coding-pack/status
# form:   /plugins/plugin-coding-pack/execute
# stream: /plugins/plugin-coding-pack/logs

[prerequisites]
claude-cli = { check = "claude --version", install_hint = "npm install -g @anthropic-ai/claude-code" }
git        = { check = "git --version", min_version = "2.20" }
npx        = { check = "npx --version", install_hint = "Install Node.js >= 18: https://nodejs.org" }

[config.defaults]
"provider-claude-code.default_model" = "sonnet"
"provider-claude-code.max_budget_usd" = 10.00
"bmad-method.communication_language" = "English"
"bmad-method.user_name" = "Jack"
"memory.provider" = "gitnexus"               # gitnexus | greptile | none
"memory.auto_reindex" = true                  # re-index after each commit
